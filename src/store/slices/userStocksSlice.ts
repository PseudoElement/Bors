import { createSlice } from "@reduxjs/toolkit";

const state = {}

const userStocksSlice = createSlice({
    name: 'userStocks',
    initialState: state,
    reducers: {
        getUserStocks() { }
    }
})
